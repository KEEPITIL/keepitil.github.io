# Runtime-3D mode (render-path A)
GLTF/FBX skeletons + clips for hero/giant units go here.
soldier/  -> soldier.gltf (+ .bin/textures), skins per era, clips named per ../../dev/ASSET-SPEC-skeletal.md
giant/    -> giant.gltf
Runtime 3D requires a skeletal runtime (three.js) — Kode wires this when assets arrive; sprite mode (../sprites) needs no runtime.
